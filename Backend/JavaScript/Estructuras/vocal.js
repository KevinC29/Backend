let v = 'Z', out = '';

if (v === 'a' || v === 'A') {
    out = `${v} es Vocal`;
}else if(v === 'e' || v === 'E'){
    out = `${v} es Vocal`;
}else if(v === 'i' || v === 'I'){
    out = `${v} es Vocal`;
}else if(v === 'o' || v === 'O'){
    out = `${v} es Vocal`;
}else if(v === 'u' || v === 'U'){
    out = `${v} es Vocal`;
}else{
    out = `${v} no es Vocal`;
}

console.log(out);